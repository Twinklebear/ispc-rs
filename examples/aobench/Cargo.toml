[package]
name = "aobench"
version = "0.1.0"
authors = ["Will Usher <willusher.life@gmail.com>"]
build = "build.rs"

[dependencies]
image = "0.20.1"
rand = "0.5"
ispc = { path = "../../" }

[build-dependencies]
ispc = { path = "../../" }

